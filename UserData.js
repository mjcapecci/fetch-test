/* This data will live for the duration of the server
and it is meant to test a single user - testing multiple users would require
a database, or session functionality at minimum */

const UserData = [];

module.exports = UserData;
